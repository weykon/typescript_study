{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "TS-Run",
            "type": "node",
            "request": "launch",
            "program": "${workspaceRoot}/dist/genericity/初体验.js",
            "args": [],
            "cwd": "${workspaceRoot}",
            "protocol": "inspector",
        }
    ]
}